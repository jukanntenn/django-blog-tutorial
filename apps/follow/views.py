from django.shortcuts import render


# Create your views here.
# 关注某个帖子
def follow(request, content_type_id, object_id):
    # A view for follow any object.
    # if already followed , cancel follow it
    pass
